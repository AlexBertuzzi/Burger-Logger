<h1>Burger Logger!</h1>

<h2>Burgers yet to be Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a New Burger.</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">New Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Has this Burger been Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> This Burger has been Devoured!<br>
    <input type="radio" name="devoured" value="0"> This Burger is yet to be Devoured!
  </div>

  <button type="submit">Add Burger</button>
</form>
